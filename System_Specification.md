# EnvPortal 系统需求与设计说明

当前设计版本：`2.6.12`

## 1. 系统定位

EnvPortal 是一个面向运维、实施和内部支持人员的轻量级环境档案门户，用于集中维护和检索测试环境、生产环境、远程连接、数据库连接、登录凭据、自由 TAG 和角色权限。

系统使用 Python 内置 HTTP 服务承载静态页面和轻量接口，前端为原生 HTML、CSS、JavaScript，数据层使用本地 CSV 与 JSON 文件。该设计目标是部署简单、便于远端服务器通过 Git 自动更新，并保留现场数据文件不进入版本管理。

## 2. 页面构成

### 2.1 环境检索 `index.html`

* **用途：** 检索和维护测试、演示、社内、受入等环境信息。
* **主要能力：**
  * 按组织、环境组、环境三层展示数据。
  * 环境区分使用枚举 `環境種別`，取值为 `社内`、`本番`；用途使用枚举 `用途`，取值为 `生産`、`開発`、`テスト`、`受入`。这两个维度在界面中显示为独立维度标记，不作为普通 TAG 过滤项。
  * 未设置环境组的环境归入 `デフォルト` 环境组，允许维护空组；创建环境时必须选择环境组，默认选择 `デフォルト`。
  * 管理员可在机构标题操作区直接新增环境组。
  * 具备编辑权限的用户可在机构标题操作区直接新增环境；管理员可在摘要分组标题区直接改名环境组。
  * 环境追加弹窗提交后必须立即调用环境检索批量保存接口持久化新增环境行，保存成功后再进入该环境的编辑态；保存失败时恢复提交前的前端状态。
  * 管理员可通过环境组容器右上角的上下箭头调整同一组织内的组显示顺序。
  * 分组、TAG 和同组环境卡片等产品相关显示排序使用固定顺序：`UPDS-V6 < UHR < UPDS-V7 < PHR`；未知项排在已知产品之后。
  * 管理员可将整个环境组迁移到其他组织；组内环境、环境 TAG 关联和可匹配的远程连接组织名会随迁移更新。目标组织如果只有一个空 `デフォルト` 组，新增或迁入其他组后会自动删除该空默认组。
  * 所有环境组均允许改名。空环境组允许删除；如果删除的是最后一个组，系统会自动重建 `デフォルト`。非空组必须先移动环境后再删除。
  * 环境组排序按钮在第一个组禁用上移，在最后一个组禁用下移。
  * 环境组移动通过环境卡片标题区的独立图标按钮和弹窗完成，保存后重新渲染当前组织画面。
  * 环境组新增、改名、删除、排序、整体迁移，以及环境所属组移动均要求系统管理权限。
  * 机构摘要在收缩态按环境组画成容器，分组信息对查看人员可见。
  * 支持环境 URL、登录 ID、密码、数据库信息、服务器信息和自由 TAG。
  * 服务器信息为可增删的列表条目，每条包含 `構築環境名`、`サーバ名`、连接类型、远程用户、远程密码和连接地址。`構築環境名` 作为服务器信息所属环境的显式归属字段，支持同一环境维护 AP、DB、踏み台或其他多台服务器；`サーバ名` 可用于 AP、DB、AP/DB 共用、踏み台或自定义名称。
  * 具备环境查询权限的用户可查看其角色数据权限范围内的完整环境信息，包括登录密码、DB 密码和远程连接凭据。
  * 支持按 TAG 多选 AND 条件过滤，过滤结果默认保持摘要态，点击展开按钮后才显示环境详情。管理员在全局 TAG 过滤结果中仍可使用机构编辑、环境追加和组追加。
  * TAG Clear 只清除用户手工选择的 TAG 条件。角色数据权限 TAG 继续作为基础数据范围生效，且页面回到普通组织摘要布局。
  * 支持按 TAG 分类分行展示过滤按钮。
  * 组织选择面板默认展开，通过顶部五十音链接切换组织列表。五十音索引允许按项目换行，较长标签作为整体换行显示。
  * 支持复制字段、打开 URL、下载 RDP、浏览器远程控制。
  * 手机触摸设备打开 Guacamole 浏览器远程控制时，前端先创建移动端远程外壳页，尽量进入全屏并锁定横屏，再加载 Guacamole；后端为 QuickConnect 和 REST 临时连接统一启用 `resize-method=display-update`，让远端桌面跟随手机横竖屏视口更新分辨率。
  * 支持首屏优先渲染公开摘要，再用已授权身份刷新敏感数据。
  * 展开的环境卡片可在原位置进入编辑态，并保留展开上下文。
  * 编辑态环境卡片被折りたたみ按钮收回时必须退出编辑态；若检测到环境字段、TAG 或服务器信息有未保存修改，应先提示用户是否保存。选择保存时必须等待保存接口成功后再收回，选择不保存时丢弃当前编辑 DOM 并恢复摘要态。

### 2.2 生产环境 `production.html`

* **用途：** 作为生产环境页签的兼容入口，跳转到 `index.html?mode=production`。
* **主要能力：**
  * 生产环境页签复用环境检索的组织、环境组、环境卡片、服务器信息和保存格式。
  * 仅显示 `data.csv` 中 `用途=生産` 的环境记录，因此它是环境检索页签的子集视图。
  * 拥有 `canViewProduction` 的角色在环境检索页也可以看到其可见范围内的本番环境记录；环境检索入口对 `canViewPortal` 或 `canViewProduction` 任一权限开放。
  * 本番环境页签的组织列表和组织摘要只根据当前可见的本番环境生成，完全没有本番环境卡片的空机构不显示。环境检索页仍允许显示和维护空机构。
  * 旧 `production.csv` 中尚未迁移的记录在 `portal_data.jsp` 中只读映射为 `用途=生産` 的统一环境记录，并把 AP、DB、VPN、踏み台字段转换为服务器信息用于显示。读取页面不会改写旧文件；管理员保存环境后才进入新的统一数据格式。
  * 新增生产环境时固定写入 `環境種別=本番`、`用途=生産`。
  * 根据 `canViewProduction` 和 `canEditProduction` 决定是否可查看和编辑。

### 2.3 用户管理 `user-admin.html`

* **用途：** 管理访问过系统的域用户和其角色。
* **主要能力：**
  * 展示用户、显示名、角色、首次访问、最后访问。
  * 支持修改显示名和角色。
  * 支持删除多余用户。
  * Windows 机器账号不作为人工用户维护。规范化用户名以 `$` 结尾的账号会被后端排除，不自动登记，也不在用户管理和角色管理数据中显示。
  * 仅具备系统管理权限的用户可访问。

### 2.4 角色管理 `role-admin.html`

* **用途：** 维护角色的功能权限和数据权限。
* **主要能力：**
  * 维护角色 ID 和显示名。
  * 维护环境查询、环境编辑、本番查询、本番编辑、系统管理等功能权限。
  * 维护 `dataTags` 数据权限。
  * 每个角色使用两行布局：第一行是功能权限，第二行是数据权限 TAG。
  * `admin` 角色受保护，不允许删除，默认拥有全部功能和全部数据。

### 2.5 代理登录 `proxy-admin.html`

* **用途：** 管理员以指定角色临时访问系统，用于验证角色权限和数据权限。
* **主要能力：**
  * 列出当前角色主数据中的可用角色。
  * 将所选角色保存到当前浏览器会话，不修改 `users.json`。
  * 受保护接口统一携带代理角色请求头，由后端验证真实管理员身份后才生效。
  * Header 显示代理登录状态、真实用户和退出代理登录按钮。
  * 退出代理登录后清除会话代理角色和认证缓存，并返回首页重新取得真实用户权限。

### 2.6 TAG 分类管理 `tag-admin.html`

* **用途：** 管理首页 TAG 过滤区域的分类展示。
* **主要能力：**
  * 管理 TAG 分类。
  * 将当前有效 TAG 分配到分类。
  * 固定 `other` 分类，删除分类后相关 TAG 自动回到 `other`。
  * 分类配置保存到 `tag_categories.json`。
  * 保存分类时保留既有 TAG 显示设定，避免覆盖 `skins` 和 `activeSkinCategory`。
  * 保存成功后通知已打开的首页重新加载 TAG 分类和配色配置，并刷新当前渲染。

### 2.7 TAG 显示设定 `tag-skin-admin.html`

* **用途：** 管理指定 TAG 对环境面板的显示皮肤。
* **主要能力：**
  * 通过顶部页签按 TAG 分类切换当前维护对象。
  * 显示当前系统正在使用的配色分类和当前正在编辑的配色分类，避免只凭页签样式判断当前生效分类。
  * 支持将当前分类设为系统渲染使用分类。
  * 为当前分类内指定 TAG 设置背景色、边框色和强调色，颜色字段提供颜色选择器和文本值同步。
  * 配置保存到 `tag_categories.json` 的 `skins[categoryId][tag]`。
  * 未填写完整三项颜色的 TAG 不改变环境面板显示。

## 3. 数据文件

| 文件 | 用途 | Git 管理 |
| --- | --- | --- |
| `data.csv` | 环境档案主数据 | 忽略 |
| `env_groups.json` | 组织下环境组主数据、空组和组顺序 | 忽略 |
| `rdp.csv` | 远程连接档案 | 忽略 |
| `production.csv` | 旧生产环境数据，保留为遗留接口兼容文件 | 忽略 |
| `tags.json` | 环境自由 TAG | 忽略 |
| `tag_categories.json` | TAG 分类、TAG 归属和 TAG 显示设定 | 忽略 |
| `users.json` | 域用户、显示名、角色和访问审计 | 忽略 |
| `roles.json` | 角色权限主数据 | 忽略 |
| `org_readings.js` | 组织读音缓存 | 忽略 |
| `.env` | 端口、认证代理、Guacamole 等部署配置 | 忽略 |
| `db_versions.json` | DB 类型和版本候选 | 纳入 Git |
| `i18n.js` | 日文和中文资源 | 纳入 Git |

现场数据文件必须保留在部署目录中，由运行时读取和写入。Git 更新不得覆盖现场数据。

### 3.1 环境组数据

`data.csv` 的 `環境グループ` 字段继续作为环境所属组字段。历史数据中该字段为空时，前端和后端都按 `デフォルト` 处理，保存时会写回归一化后的组名。

`data.csv` 新增兼容字段 `環境種別` 和 `用途`。旧文件缺失字段时按空值读取，前端会从旧结构 TAG 推断初始值：`本番`、`生産`、`生产` 推断为 `用途=生産`，`開発`、`开发` 推断为 `用途=開発`，`テスト`、`测试` 推断为 `用途=テスト`，`受入` 推断为 `用途=受入`。后续保存会写回显式枚举值。旧 `production.csv` 作为现场遗留数据继续保留，后端读取 `portal_data.jsp` 时会把未迁移的旧本番记录转换到统一环境响应中；该兼容转换不在 GET 请求中写文件。

`rdp.csv` 保存服务器信息。`構築環境名` 是兼容新增字段，用于把服务器信息显式归属到同一组织下的指定环境；新保存的 RDP 行必须写入该字段。旧 CSV 缺失该字段时按空值读取，并继续通过环境 URL 主机和 DB 主机做兼容匹配。`scripts/migrate-rdp-env-links.ps1` 用于受控升级旧 RDP 行：同组织内能通过 URL 主机或 DB 主机唯一匹配环境时补齐 `構築環境名`；没有任何候选主环境、缺少组织/目标地址、已有 `構築環境名` 但主环境不存在、或匹配到多个候选主环境时，均输出到脏数据报告，脚本不自动删除这些行。`サーバ名` 是兼容新增字段，用于环境卡片中服务器信息的显示标题；旧 CSV 缺失该字段时按空值读取，并在后续保存时补齐。显示时优先使用 `サーバ名`，没有值时按 AP/DB 匹配结果给出默认标题。

`env_groups.json` 保存组织下的显式环境组列表和空组。结构为 `records[orgKey] = { key, code, name, groups }`。前端加载时会合并 `env_groups.json`、`data.csv` 中已有环境组和默认组，因此缺失文件或缺失组织记录不会影响旧数据读取。

`デフォルト` 是自动兜底组名，不作为不可改名的保留名称。所有环境组均可显示、选择和改名。删除某个环境组内最后一个环境后，组仍保留在 `env_groups.json`，由管理员在组织层继续维护。空组可删除；如果删除后该组织没有任何分组，系统会自动重建 `デフォルト`。

## 4. 认证与身份

### 4.1 基本策略

系统以域用户作为身份标识。客户端 IP 仅用于显示、审计和辅助排查，不能作为授权依据。

### 4.2 域认证代理

当 EnvPortal 部署服务器未加入 AD 域时，可在已加入域的 Windows 主机上部署 EnvPortal Domain Proxy。代理通过 Windows Integrated Authentication 获取当前域用户，再将可信认证结果提供给 EnvPortal。

前端可通过跨域探测获取一次域用户，EnvPortal 随后签发短期认证 token。浏览器缓存 token 后，后续受保护接口直接访问 EnvPortal，减少重复 Windows 认证等待。

### 4.3 用户档案

首次访问的域用户自动写入 `users.json`。如果 AD 可返回 `displayName`、`mail`、`department`、`title`，系统同步保存这些字段。无法读取 AD 属性时，至少保存域账号。

运行 JSON 文件统一通过结构化安全读写函数处理。`users.json`、`roles.json`、`tags.json`、`tag_categories.json`、`env_groups.json` 读取失败时，后端会尝试读取最近的有效 `*.bak*` 备份，避免把解析异常误判为空数据。写入时会生成 `*.bak_autosave_*` 备份，再用临时文件替换。用户保存会过滤 Windows 机器账号。

环境检索运行 CSV 也使用受保护写入。`data.csv`、`rdp.csv` 和 `update_portal_bundle.jsp` 的 CSV 内容保存会先复制当前文件为 `*.bak_autosave_*`，再写入唯一临时文件并替换目标文件，避免空包、半写入或并发写入把现场 CSV 直接覆盖到不可恢复状态。

`update_portal_bundle.jsp` 会校验 `data.csv` 的 CSV 头和行数。现有 `data.csv` 非空时，如果提交内容为 0 行，后端返回错误并拒绝覆盖，避免前端异常状态把现场环境数据保存为空。

## 5. 权限模型

### 5.1 功能权限

角色支持以下功能权限：

* `canViewPortal`：可查询环境检索。
* `canEditPortal`：可编辑环境检索数据。运行时仅 `admin` 角色有效。
* `canViewProduction`：可查询生产环境。
* `canEditProduction`：可编辑生产环境。运行时仅 `admin` 角色有效。
* `canManageUsers`：可进入系统管理菜单并维护用户、角色、代理登录、TAG 分类、TAG 显示设定和环境组管理操作。运行时仅 `admin` 角色有效。

旧字段 `canEdit` 保留为兼容聚合字段。新逻辑以具体功能权限为准。
角色数据权限 TAG 按 TAG 分类组合：同一分类内多个 TAG 按 OR 处理，不同分类之间按 AND 处理。这样产品类可授权多个产品，同时客户组、系统等业务分类可继续收窄数据范围。
系统管理菜单的显示只由当前用户认证结果控制。首屏快速数据载入不能覆盖系统管理菜单状态，认证接口应刷新本地缓存以避免旧角色缓存影响管理员菜单。

`FORCED_ADMIN_USERS` 定义永久管理员账号，默认包含 `x02851`。永久管理员账号在用户自动登记、访问刷新和用户管理保存时都必须保持 `admin` 角色。

页面上的追加、编辑、删除、移动、保存等操作按钮必须使用管理员身份和页面级编辑权限共同控制。环境检索要求 `role=admin` 且 `canEditPortal=true`，生产环境模式要求 `role=admin` 且 `canEditProduction=true`，不能用旧的汇总字段 `canEdit` 代替页面级权限。生产环境模式只能新增和删除 `用途=生産` 的环境记录。非管理员角色即使残留或提交编辑权限字段，后端也必须规范化为只读。

顶部功能导航必须按查看权限控制。没有 `canViewPortal` 时隐藏环境检索入口，没有 `canViewProduction` 时隐藏生产环境入口。

RDP 解锁密码框必须放在标准 `form` 中，并提供 label、name 和 autocomplete 属性，避免浏览器密码表单检查产生警告。

### 5.2 数据权限

环境检索的数据权限使用 `dataTags` 数组控制。非管理员角色只显示命中 `dataTags` 中任一有效 TAG 的环境。非管理员角色的有效 `dataTags` 覆盖当前全部有效 TAG 时，运行时视为全量数据范围，只保留功能权限限制。有效 TAG 只由 `tags.json` 中手工维护的业务 TAG 组成，不再从环境组、环境名、环境区分、用途、DB 类型、DB 版本或远程连接类型自动抽取。升级兼容期内，旧数据中残留的结构性 TAG 仍可用于既有角色匹配，但首页 TAG 过滤、编辑框和显示皮肤不再把它们作为普通 TAG 展示。

同一个环境可以包含多个 TAG。数据可见性采用“命中任一授权 TAG 即可见”，但首页 TAG 过滤器只展示当前角色授权的 `filterTags`，避免未授权 TAG 被点选后改变授权范围。所有前端过滤都会先应用角色 `filterTags` 基础范围，再把用户选择的 TAG 作为 AND 条件追加。
TAG 过滤器的清除操作只清除用户选择的追加 TAG 条件，不能清除角色 `filterTags` 基础范围。

如果角色配置中存在已经删除或当前不存在的 TAG，该 TAG 不授予任何数据可见性。旧字段 `filterTag` 保留兼容，读取旧配置时会迁移为 `dataTags`，保存时以 `dataTags` 为准。

### 5.3 默认角色

* `admin`：全部功能权限和全部数据权限。
* `staff`：默认可进入环境检索，数据权限需手工分配。
* `import_staff`：默认绑定 `OneHR` 数据 TAG。
* `new_employee`：默认绑定 `社内学習` 数据 TAG。

## 6. TAG 设计

### 6.1 手工 TAG

手工 TAG 保存在 `tags.json`，按环境行 key 关联环境。编辑环境时可维护 TAG。

### 6.2 自动 TAG

系统不再从环境组、环境名、数据库字段或远程连接字段自动抽取 TAG。首页过滤、角色数据权限、TAG 分类和 TAG 显示设定都只使用手工 TAG。

### 6.3 TAG 分类

TAG 分类保存在 `tag_categories.json`。首页按分类显示 TAG 过滤按钮，未知或未分类 TAG 进入 `other` 分类。`other` 为固定分类，不允许删除。

### 6.4 TAG 显示设定

TAG 显示设定保存在 `tag_categories.json` 中。`skins` 节点结构为 `skins[categoryId][tag] = { bg, border, accent }`。`activeSkinCategory` 表示当前系统用于首页环境面板渲染的 TAG 分类。该结构按分类隔离同名 TAG 的皮肤定义，避免不同分类体系切换后发生渲染冲突。

首页环境面板渲染时，会先取得环境的全部手工 TAG 和自动 TAG，再只在 `activeSkinCategory` 对应分类内查找已配置的皮肤并应用到环境面板。收缩摘要卡片、展开详情卡片和命中的 TAG 过滤按钮都应用同一套皮肤。未配置皮肤的 TAG 不改变面板显示。

已应用皮肤的环境面板直接显示背景色、边框色和强调线，不额外显示系统设定标签。TAG 显示设定入口保留在管理员可见的系统管理菜单中。

系统为常用产品版本提供浅色默认皮肤：UHR 为浅绿，PHR 为浅蓝，UPDS-V6 为浅灰，UPDS-V7 为浅橙。默认皮肤在运行时归入该 TAG 当前所属分类。

## 7. 加载与渲染策略

* 首页优先渲染公开摘要，避免等待域认证完成后才显示页面。
* 获取有效域 token 后，再刷新为授权数据。
* 域 token 在浏览器本地短期缓存，减少跨页管理功能等待。
* `portal_config.jsp` 不阻塞等待 Guacamole 连通性探测；接口立即返回最近一次缓存状态，并在后台线程刷新 Guacamole 可用性。
* 首页首屏优先渲染环境摘要列表，组织选择和 TAG 过滤面板在首屏摘要完成后异步补充渲染。
* 前端对行级 TAG、面板皮肤、组织分组和 RDP 主机/端点匹配建立派生缓存；当 `fullData`、`rdpData`、`tagStore` 或 TAG 分类配置变化时必须重置缓存。
* 连通性检查不阻塞首屏渲染，页面加载完成后再进入检查队列。
* 服务器面板使用响应式网格，低宽度桌面一行最多两个，手机竖屏一行一个。
* 环境卡片操作集中在同一按钮组中，复制、编辑、组移动、保存、取消、删除、展开和收缩均使用图标按钮；展开态的收缩按钮直接随卡片模板渲染到与编辑、复制相同的按钮组内，并通过按钮自身上下文收回对应卡片；编辑、取消、保存刷新卡片时保留展开态上下文，运行状态显示在标题区域，按钮组右侧顶端对齐。
* 组织详情中的环境组容器在桌面最多显示两列环境卡片，窄屏显示一列，避免展开态四列挤压导致内容换行失控。

## 8. 远程部署与自启动

20.38 服务器通过 Git 更新主分支，并注册为 Windows 自启动服务。部署现场数据文件被 `.gitignore` 排除，防止自动更新时覆盖或阻塞拉取。

服务状态要求：

* Windows Service 名称：`EnvPortal`
* 启动类型：Automatic
* 工作目录：EnvPortal Git 工作区
* HTTP 服务默认端口：由 `.env` 的 `PORT` 决定，当前部署使用 `8999`

## 9. 版本策略

项目使用语义化版本：

* `MAJOR`：不兼容的运行方式、数据结构或主要交互变化。
* `MINOR`：兼容性功能新增。
* `PATCH`：缺陷修复、样式修正、文案和资源调整。

`2.3.0` 是一次功能重编号版本。原因是 `2.2.16` 到 `2.2.31` 连续加入了系统管理、域认证 token、角色功能权限、角色数据权限、TAG 分类管理、i18n 补齐和加载性能优化。这些能力组成一个兼容性功能集合，符合 MINOR 递进。现场数据文件新增了 `roles.json` 和 `tag_categories.json`，并提供默认生成和旧字段兼容逻辑，因此不构成 MAJOR 级别迁移。

`2.4.0` 是环境组三层层级的功能版本。它新增 `env_groups.json` 用于保存空组和组顺序，并完善默认组、环境移动、空组删除和摘要分组容器。该版本不改变 `data.csv` 字段结构，历史空 `環境グループ` 仍兼容，因此属于 MINOR 递进。

`2.6.0` 是生产环境格式合并和环境枚举维度版本。它将生产环境页签改为 `data.csv` 中 `用途=生産` 的统一环境卡片视图，并新增兼容字段 `環境種別`、`用途`。旧数据可继续读取并由前端推断默认值，因此属于 MINOR 递进。

`2.6.1` 是环境组维护规则修正版。它允许包括 `デフォルト` 在内的所有组改名，并允许删除最后一个空组，删除后自动重建 `デフォルト`，因此属于 PATCH 递进。

`2.6.2` 是环境组摘要操作区的 UI 修正版，并增加项目级验证规则。所有修改必须执行相关测试；UI 修改必须启动页面或目标环境，完成浏览器检查、控制台检查和截图验证，因此属于 PATCH 递进。

`2.6.3` 是角色编辑权限收敛修正版。它将环境编辑、生产编辑和系统管理权限固定为 `admin` 角色专属，非管理员角色保留查看权限和数据权限，所有编辑字段在后端规范化时强制无效化，因此属于 PATCH 递进。

`2.6.4` 是旧本番数据兼容修正版。它恢复 `production.csv` 旧现场数据在统一生产环境页签中的可见性，并按生产查看权限过滤转换结果，不在读取页面时改写现场旧文件，因此属于 PATCH 递进。

`2.6.5` 是生产环境子集语义修正版。它把环境检索入口视为包含本番数据的上层入口，允许拥有本番查看权限的角色进入环境检索并看到本番记录，同时在本番页签隐藏没有本番记录的空机构，因此属于 PATCH 递进。

`2.6.6` 是环境卡片编辑态收回行为修正版。它为收回按钮增加未保存修改检测、保存确认和保存后收回流程，避免编辑态 DOM 被直接折叠后留下状态不一致，因此属于 PATCH 递进。

`2.6.7` 是多服务器归属修正版。它为 `rdp.csv` 增加兼容字段 `構築環境名`，让同一环境下 AP、DB、踏み台或其他服务器信息可以稳定保存和读回；旧 RDP 行继续使用 URL/DB 主机匹配作为兼容路径，因此属于 PATCH 递进。

`2.6.8` 是旧 RDP 数据归属升级工具版本。它增加受控脚本和后端推断函数，自动补齐可唯一确定主环境的旧 RDP 行，并将无法归属或归属不唯一的行报告为脏数据，因此属于 PATCH 递进。

`2.6.9` 是手机端 Guacamole 横屏适配版本。它补齐 QuickConnect 的 RDP 动态分辨率参数，并在移动端使用远程外壳页承载 Guacamole，降低手动横屏和浏览器自动旋转叠加导致的缩放异常，因此属于 PATCH 递进。

`2.6.10` 是 TAG 过滤结果页管理员操作区修正版。它让全局 TAG 过滤结果页复用机构级操作区，过滤只影响数据集合，不改变管理员维护入口，因此属于 PATCH 递进。

`2.6.11` 是组织选择五十音索引显示修正版。它将索引项改为链接式换行布局，让较长标签整体换行，避免窄侧栏中的截断和横向滚动，因此属于 PATCH 递进。

`2.6.12` 是角色全选数据权限 TAG 的范围修正版。它将覆盖当前全部有效 TAG 的非管理员角色视为全量数据范围，避免跨分类 AND 规则把全选解释为组织必须同时覆盖所有 TAG 分类，因此属于 PATCH 递进。

## 10. 多语言用语规则

日语 UI 文案应优先使用日语中自然的片假名或惯用表达。已有常用表达的英文原文不要直接显示，例如 `TAG` 在日语 UI 中写作 `タグ`，`Version` 写作 `バージョン`，`Platform` 写作 `プラットフォーム`。

允许保留原文或英文简称的场景：

* 协议、产品名或技术缩写，例如 URL、DB、RDP、SSH、VPN、AD、HTTP、TTL、OS。
* 第三方产品或专有功能名，例如 Guacamole、QuickConnect。
* 数据字段名、文件名、JSON key 和代码标识，例如 `dataTags`、`tag_categories.json`。
